# 药材的烘干问题：数学建模竞赛详细指导计划

## 1. 方案与阅读顺序

**推荐主线：圆柱径向传热传质模型 → 变物性热湿耦合模型 → 全域含水率阈值事件 → 基于实测半径的收缩域模型。**

本计划依据题面《2026 年高教社杯全国大学生数学建模竞赛 A 题：药材的烘干问题》及配套附件，按国赛机理建模题组织，供小组分工、建模、编程和论文撰写使用。

当前已完成四问方案设计、第一问基准与灵敏度，以及第二问0–72 h变物性热湿耦合求解。第二问表3–4、`result2.xlsx`、图源和验证归档已生成；正式网格N=20480。第三、四问尚未运行，`result3`–`result4`及严格烘干时长没有数值结论。

小组起步方案（可在实施前调整并记录）：

- 竞赛类型：按所提供文件的国赛题面组织。
- 指导文档语言：中文；后续论文语言与排版遵从实际比赛要求。
- 排版引擎：本阶段不依赖排版引擎，论文阶段再选 Typst 或 LaTeX。
- 子问题数量：明确编号的 4 问，保持四问递进关系。
- 计算工具：下面以 Python 的实现接口说明，也可用 MATLAB 实现同一组离散方程；无需同时开发两套完整程序。
- 工作优先级：先实现可解释、可验证的四问共同模型，再增加有证据支持的改进。

阅读顺序：

1. 本文件：了解四问路线、任务先后、分工、图表和风险。
2. [建模说明](reports/ANALYSIS_MODELING_REPORT.md)：查看可直接转成代码的方程、边界条件、离散格式和检验方法。
3. [待办清单](todo.md)：按任务逐项推进；其中未勾选项均为后续工作。

## 2. 从评阅角度抓住题目核心

本题的主要任务是解释并预测给定工况下药材内部的温度、水分分布和达标时间。背景提到效率、能耗和品质，但四问没有给出能耗函数、品质评价数据或可优化的控制范围，不宜另外搭建缺少依据的多目标优化模型。

论文应形成以下证据链：

| 评阅者需要理解的事 | 应给出的证据 |
| --- | --- |
| 为什么用空间模型 | 几何分析、径向 Biot 数、中心与表面的差异 |
| 方程如何来自物理过程 | 控制体推导、初始条件、对流热流和水分通量 |
| 四问是否递进 | 从常物性到变物性，再到事件判定和收缩域 |
| 计算是否可信 | 网格收敛、时间误差检查、简化问题基准、通量平衡 |
| 结果是否回答题目 | 表 1–6、四份 Excel、全域达标时间及单位 |
| 哪些结论依赖假设 | 长期环境、传质边界、潜热及收缩模型的对照 |

不需要为了方法数量引入神经网络、AHP、熵权法或遗传算法。当前附件也没有药材内部温度和含水率的实测标签，不能声称完成了内部场的实验拟合与验证。

## 3. 附件已经确认了什么

| 材料 | 实际内容 | 用途与注意事项 |
| --- | --- | --- |
| `A题/A题.pdf` | 4 页，4 个问题，4 个附录 | 方程参数取自 PDF 附录；“附件2”和“附录2”不同 |
| `附件1.xlsx` | 241 条数据，时间、温度、水分浓度三列；0–14400 s，每隔 60 s | 烘房环境边界，覆盖 4 h，不能直接代表数天数据 |
| `附件2.xlsx` | 145 条数据，时间、半径两列；0–259200 s，每隔 1800 s | 第四问半径，覆盖 72 h；半径由 2 cm 降至 1.198 cm |
| `result1.xlsx`、`result2.xlsx` 模板 | 各有“温度”“水分浓度”两张表 | 首行半径 cm，首列时间 s；省略号必须展开 |
| `result3.xlsx` 模板 | 一张水分浓度表 | 每隔 60 s 输出，首行半径 cm |
| `result4.xlsx` 模板 | 一张水分浓度表，末列“药材表面” | 表面位置随时间改变，不能当作固定 2 cm |

两份数据的数值区域未发现缺失、重复时间或时间间隔异常；附件2半径非增。烘房最后 1 h 的温度均值约为 **49.9989 °C**，水分浓度均值约为 **0.0499875 kg/kg**，为后续恒温阶段采用约 50 °C、0.05 kg/kg 提供了依据。这是边界延拓假设的依据，并非题面直接给出的永久工况。

## 4. 四问的实施路线

| 问题 | 核心任务 | 主模型与求解 | 应交付的结果 | 主要检查 |
| --- | --- | --- | --- | --- |
| 1 | 预测前 1800 s 内部温度和水分 | 一维圆柱径向导热、非线性水分扩散；常热物性、第三类边界；有限体积法 | 题面表 1–2；result1 两张表，时间 1–1800 s，半径 0–2 cm，每隔 0.1 cm | 中心条件、表面通量、温度和含水率范围、网格误差 |
| 2 | 建立全过程热湿耦合模型 | 统一使用附录3的四个经验公式，从初始时刻重新求解 | 题面表 3–4，0.5–3 h；result2 每隔 1 s 的温度与水分场 | 开尔文转换、局部物性更新、变系数散度、长期环境 |
| 3 | 求固定尺寸下全域达标时间 | 延续第二问方程；检测最大含水率越过 0.15 的事件 | 烘干时长 h、表 5、result3 每隔 60 s | 不以平均值或表面值替代全域最大值；末时刻精确定位 |
| 4 | 考虑收缩重新求解 | 用附件2重建 R(t)，换用附录4物性，映射至固定坐标求解 | 烘干时长 h、表 6、result4 含动态表面列 | 材料坐标一致性、实际半径取值、域外空白、物性与几何分离 |

**第二问输出终点已固定。** 本轮按用户批准的交付契约计算0–72 h，`result2.xlsx`保存1–259200 s，内部归档保留t=0。正文表3–4仍只抽取0.5–3 h。该范围是长期环境延拓下的第二问情景，不预先宣称等于问题三的严格烘干结束时刻；问题三应从全域最大含水率事件独立判定。

### 4.1 问题一：先把物理边界和数值求解做对

正式实现采用[Q1模型契约](reports/Q1_MODEL_SPEC.md)，由Codex负责建模与数值实现，小组另行交叉审核。该契约细化本节：中截面热端面影响用条件性解析上界；只用0–1800 s线性环境；谐均面扩散系数、含表面储存的节点FVM及两个三对角Jacobian块；60 s分段BDF、Radau复核；完整级数和变D制造场先行。网格从40/80/160倍增，原上限10240只能提供一次达标细化，经用户明确授权扩至20480后，连续两次全输出及早期表面细化差进入预算。温度/含水率总数值目标1e-3 °C/1e-5 kg/kg及80%/20%预算均未放宽；空间组因时间误差而收紧，实际记录见验证报告。不增加第一问D的温度依赖或显式潜热。验证通过后已生成正式结果表。

1. 统一单位：长度 m、时间 s；保存表格时再转换为 cm 和 h。
2. 定义径向位置 r、温度 θ(r,t) 和干基含水率 C(r,t)。
3. 采用长圆柱的一维径向近似，明确端面影响被忽略；必要时用二维轴对称模型作小规模对照。
4. 写出导热方程与非线性扩散方程，中心为对称条件，表面为对流条件。
5. 环境温湿数据用分段线性插值；不以药材表面温度等于烘房温度代替对流换热。
6. 构造含中心、表面的节点控制体；先用固定扩散系数小算例检查符号和边界，再使用题给 D(C)。
7. 在内部较细网格求解，独立按题目要求抽样输出。
8. 生成表 1–2 和完整矩阵，并解释中心升温滞后、表面失水及径向差异。

第一问基准已完成；第二问只复用其已审计的径向网格定义，并独立实现变物性耦合、长时积分、归档和导出。第三、四问可继续继承已验证的控制体几何与输出口径。

### 4.2 问题二：真正实现双向耦合

1. 将密度、比热、导热系数和扩散系数替换为附录3公式。
2. 每个网格点按当地 θ、C 更新物性，D 中必须使用 T = θ + 273.15。
3. 热方程保留 `散度(k × 温度梯度)`，水分方程保留 `散度(D × 含水率梯度)`。
4. 从 t=0、θ=28 °C、C=2.55 重新积分；不在 1800 s 任意切换附录2和附录3。
5. 在 4 h 内使用附件1；4 h 以后用末段均值延拓，并做替代工况对照。
6. 使用隐式时间推进，避免细网格和长时间尺度导致显式算法过慢。
7. 输出题目规定的 6 个时刻、5 个半径的温度和水分表。
8. 比较第一问与第二问在共同时间范围内的结果差异，解释物性变化的作用。

Python 可采用空间离散后的 `solve_ivp` 隐式方法；`BDF`、`Radau`、事件定位和稀疏 Jacobian 均有正式接口。具体接口以 [SciPy solve_ivp 文档](https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.solve_ivp.html)为准。是否足够精确仍需本题的收敛检查。

### 4.3 问题三：把“各处达标”转成事件

本次实施细化见[问题三模型规范](reports/Q3_MODEL_SPEC.md)：使用N=5120、10240、20480事件网格对照，保存完整根状态；复用已审查的Q2物理算子，在独立驱动中从原始初值重算，不将21半径归档当作全域状态。Q2文件保持其72 h交付范围。

1. 每个计算时刻计算全域 `Cmax(t)`，同时记录其位置。
2. 构造 `g(t)=Cmax(t)-0.15`，定位从正到负的首次阈值穿越。
3. 在预计结束时间附近加密，给出穿越前后状态及数值误差；不能只按小时或分钟取首个达标点。
4. 将最大值、中心值、表面值、体积平均值画在一张图中，体现各种停机判断的差异。
5. 即使验证了中心始终最湿，程序仍保留全域最大值检查。
6. 输出每隔 6 h 的论文表，以及每隔 60 s 的完整场；额外附上准确结束时刻，避免遗漏末状态。
7. 用 2–3 天作为量级审查参考；若不在此范围，先核查模型和假设，不能调参把结果强行压入该区间。

### 4.4 问题四：收缩模型和物性必须一起更新

1. 从附件2重建连续、非增、正值的 R(t)，优先使用保形插值；用线性插值作为对照。
2. 用 ξ=r/R(t) 将变化区域映射到 [0,1]。
3. 明确采用均匀径向收缩的材料运动假设；从物质导数推导方程，不能凭经验添加或删除对流项。
4. 更新扩散长度、边界梯度比例及表面面积；同时将四个物性公式全部换成附录4。
5. 使用与第三问相同的全域阈值规则求结束时间。
6. 用四组算例区分几何与物性作用：附录3/附录4分别搭配固定半径/实测收缩。
7. 导出时按真实距离 r 查询 ξ=r/R(t)。超过当前半径的位置留空并说明原因，不能填零。
8. “药材表面”列始终取 ξ=1；另在报告或辅助记录中保存每个时刻的实际 R(t)。

PCHIP 可以保留单调数据的形状并避免插值过冲，适合本附件的非增半径序列；其作用不包括保证区间外的外推合理性。参见 [SciPy PCHIP 文档](https://docs.scipy.org/doc/scipy/reference/generated/scipy.interpolate.PchipInterpolator.html)。

## 5. 开始求解前必须写清的假设

| 假设或歧义 | 推荐基准 | 必做或优先对照 |
| --- | --- | --- |
| 长圆柱能否忽略端面 | 以径向一维场为主体 | 端面/侧面面积比为 0.08，这不是误差界；有余力做二维轴对称核验 |
| 烘房水分浓度与药材干基含水率的基准不同 | 将附件环境水分值视作题目简化下的等效边界驱动，显式标注此闭合假设 | 讨论吸附等温线/界面平衡缺失；不能声称两种 kg/kg 物理上天然等同 |
| 是否考虑蒸发潜热 | 建立题给参数下的有效传热传质基准，暂不显式加入潜热 | 将潜热列为结构性不确定性；若要升级，补齐质量通量和相平衡，先做能量量级核查 |
| 4 h 之后的环境 | 最后 1 h 均值延拓 | 末值保持、50 °C与0.05保持；必要时做有界扰动 |
| 后三问的换热与传质系数 | 暂沿用附录2的 h 与 hm | 明确属于继承假设，分别扰动检验 |
| 第二问“阶段不同” | 附录3统一覆盖全过程 | 不擅自认定30 min为参数切换时刻；实际阶段可用环境和响应划分 |
| 第四问的收缩机制 | 径向仿射收缩、长度不变 | 不将表面移动与材料运动混为一谈；讨论长度变化资料缺失 |
| 收缩下干基含水率的守恒 | 用干固体质量作含水率的参照 | 不因体积缩小直接给 C 乘体积比；不把经验密度同时当作独立的干固体密度 |

这部分应在建模前完成。模型的可计算性来自明确假设，不能把缺失物理信息写成题面已知。

## 6. 数值验证和灵敏度实验

### 6.1 最少要完成的验证

| 类别 | 具体操作 | 可以支持什么结论 |
| --- | --- | --- |
| 方程基准 | 恒定环境、常系数扩散，与圆柱解析级数或独立离散解对照 | 空间算子、中心条件和边界符号实现正确 |
| 平衡与隔离情形 | 内外相同状态应保持不变；hm=0 时不应失水 | 极限行为和边界逻辑正确 |
| 空间收敛 | 比较40、80、160个径向区间，必要时320个 | 报表节点和结束时间对网格的依赖 |
| 时间误差 | 收紧容差、限制最大步长，复算关键算例 | 时间离散和事件定位稳定 |
| 通量平衡 | 累积水分变化与边界通量核对；收缩模型核对干固体质量口径 | 没有凭空生成或丢失模型中的水分 |
| 退化一致性 | 将第四问 R(t)固定，物性也换回附录3 | 应退化为第二、三问的同一个数值问题 |
| 报表核对 | 从同一份全精度结果抽表和导出，回读检查 | 单位、时间、半径、表面列及四位小数无错位 |

前期可用“结束时间变化小于 0.1%”作为内部稳定目标，但这不是比赛给定的准确率标准。最终必须公布真实误差；保留四位小数也不意味着模型具有四位小数的真实精度。

### 6.2 优先级最高的敏感性分析

第一问专项：以PR #2有效径向基准为基础，实施h、hm、D前因子±10%、±20%的12组单因素情景。只分析固定1800 s响应和0–1800 s全输出差，不使用烘干时长指标。详见[实施契约](reports/Q1_SENSITIVITY_SPEC.md)、[结果](reports/Q1_SENSITIVITY_REPORT.md)与[鲁棒性设计](reports/Q1_ROBUSTNESS_DESIGN.md)。以下长期驱动、事件和收缩分析仍属于后续问题，未在本轮实现。

1. h、hm 分别上下扰动 10%，保持其余条件不变，比较烘干时间、最大径向差异。
2. 附录中的 D 前因子上下扰动 10%，解释内部传质的影响。
3. 对长期温度取基准±1 °C，对长期环境水分取基准±0.002 kg/kg，作为设定场景，不能称作统计置信区间。
4. 对半径观测按显示精度进行有约束微扰，对比线性插值和 PCHIP，保持半径正值及非增性。
5. 做第四问的2×2对照，分离物性与几何影响，检查是否存在交互作用。
6. 有余力时对传质闭合、潜热和端面模型做结构性对照。这些影响若明显大于网格误差，应在论文结论中优先说明。

## 7. 三人队伍的参考分工与72小时排程

以下为训练或参赛工作量的 **72小时参考排程**，不是对正式赛程的断言。实际可按剩余时间等比例压缩。

角色建议：A负责机理与推导，B负责数值与数据，C负责论文、图表解释及独立核查。C从开始就参与条件核对；A和B交叉检查边界与单位。

| 时段 | A：机理与模型 | B：计算与数据 | C：论文与核查 | 阶段出口 |
| --- | --- | --- | --- | --- |
| 0–4 h | 题意拆解、歧义与尺度估计 | 整理单位、附件、输出模板 | 建立四问论文骨架、核对提交项 | 一页模型框架、变量表、数据字典 |
| 4–12 h | 推导问题一及边界条件 | 实现有限体积算子、跑基础算例 | 起草假设、方法说明，准备图表目录 | 问题一可运行，表1–2可自动抽取 |
| 12–24 h | 推导变物性耦合和长期边界 | 实现问题二、三及事件定位 | 整理前两问论证，核对量纲和数据来源 | 前三问完整结果与初步烘干时长 |
| 24–38 h | 推导材料坐标与收缩模型 | 实现问题四、真实半径导出 | 写第四问思路，解释边界与缺失项 | 四问主结果全部可复现 |
| 38–50 h | 分析2×2对照和结构假设 | 网格、容差、参数敏感性实验 | 汇总结果表、制作论文图 | 收敛证据、机制解释、风险排序 |
| 50–62 h | 审核推导和结论 | 冻结结果，统一导出 | 完成正文、结果讨论、最后撰写摘要 | 完整论文初稿和四份Excel |
| 62–68 h | 独立核对四问答案 | 独立回读结果表和复现日志 | 检查图文、公式、单位、参考文献 | 内容一致且无关键缺项 |
| 68–72 h | 修复实质问题 | 整理可复现材料 | 编译、检查版面、按正式规则整理提交包 | 可提交版本与保留备份 |

如果时间明显不足：优先完成四问、表1–6、四份Excel、一次有效收敛检查和第四问对照；压缩装饰性图示和额外参数实验。不能通过删除全域阈值检查或绕过收缩坐标来节省时间。

## 8. 论文结构与图表安排

建议章节：摘要；问题分析；假设与符号；共同物理基础；问题一；问题二；问题三；问题四；数值检验与灵敏度；模型评价；参考文献与必要附录。实际格式、页数和声明要求在写作阶段按正式通知核对。

| 图表 | 需要表达的结论 | 来源 |
| --- | --- | --- |
| 输入环境温度和水分双子图 | 区分升温段、接近恒定段及外推部分 | 附件1与边界方案 |
| 半径观测与插值曲线 | 显示早期快速收缩、后期平台和观测范围 | 附件2 |
| 圆柱与边界条件示意图 | 解释 r、R(t)、中心对称、表面对流 | 模型推导 |
| 前30分钟温度与含水率径向曲线 | 说明径向差异及滞后 | 问题一数值场 |
| 热湿场热力图 | 说明温度与含水率演化的不同时间尺度 | 问题二数值场 |
| 最大值/中心/平均/表面含水率曲线 | 解释全域停机标准 | 问题三结果 |
| 固定域与收缩域对照图、2×2结果表 | 分离几何和物性的影响 | 问题四与对照实验 |
| 收敛图、敏感性图 | 说明结论对数值误差和假设的依赖 | 验证实验 |
| 题面表1–6 | 直接回答所有指定数值问题 | 同一份全精度求解结果 |

每张图都要有正文中的解释。主文以清晰二维图和关键表格为主，大规模每秒数据放在题目要求的Excel中。摘要最后写，逐问写明“模型、结果、数值依据”，不提前编造时长或误差。

## 9. 后续工作流与文件产物

以下阶段为后续实施顺序；当前进度为计划与建模设计完成。角色A、B、C对应第7节的机理、计算和论文分工，由组员认领。

| 阶段 | 建议负责人 | 预期产物 | 当前状态 |
| --- | --- | --- | --- |
| 赛题分析与建模设计 | A牵头，全组核对 | `reports/ANALYSIS_MODELING_REPORT.md` | 已完成计划层面的方程、假设与接口 |
| 编程、求解与数据图表 | B牵头，A核对模型 | `q1/`、`q2/`、`results/`、`reports/RESULTS_REPORT.md`、`figures/*.pdf` | 第一、二问已完成；第三、四问待执行 |
| 必要的流程和结构图 | C绘制，A核对含义 | 模型示意图、`reports/DRAWIO_REPORT.md` | 按正文需求执行 |
| 竞赛论文 | C统稿，A与B提供推导和结果 | `paper/main.typ`或`paper/main.tex`与章节 | 待执行 |
| 验证与验收 | 全组交叉检查 | `reports/VERIFY_REPORT.md`、最终提交材料 | 待执行 |

建议代码结构：

```text
code/
  data_io.py           # 输入、单位转换、环境与半径插值
  properties.py        # 附录2/3/4的物性，分别命名
  radial_fvm.py        # 圆柱有限体积算子与边界
  solver.py            # 常物性、变物性、收缩域求解
  dry_end.py           # 全域阈值、末时刻抽样
  experiments.py       # 收敛、敏感性、2×2对照
  export_results.py    # 表1–6及result1–result4
  make_figures.py      # 由结果生成论文图
  run_all.py           # 一次运行重现全部结果
```

第一项实际开发任务应是：**用问题一搭好单位统一、径向有限体积、中心与表面对流边界、报表抽样这条完整链路。** 待基准通过验证，再推进变物性与收缩。
