# 药材烘干建模交付计划

## 目标

以一维圆柱径向有效热湿模型完成四问，交付表1–6、result1–4、数值验证、图源、模型边界和可复现说明。竞赛高奖目标依靠机理解释、全域事件、误差分层和证据闭环，不堆叠无数据支持的方法。

## 四问主线

| 阶段 | 模型 | 正式交付 | 状态 |
|---|---|---|---|
| Q1 | 附录2，短时常热物性基准 | 表1–2、result1、灵敏度 | 完成 |
| Q2 | 附录3，0–72 h 变物性耦合 | 表3–4、result2 | 完成 |
| Q3 | 固定半径全域达标事件 | 57.4740097759 h、表5、result3 | 完成 |
| Q4 | 附录4、收缩半径与全域事件 | 51.0920106837 h、表6、result4 | 完成 |

## 现行建模决策

1. Q2/Q3 使用附录3，Q4 使用附录4；所有长期问题都从原始初值积分。
2. 环境 0–4 h 逐段线性插值，之后主情景保持最后一小时均值。
3. Q4 主半径方案为分段线性插值和限幅；PCHIP、显示刻度扰动和末值环境仅作结构情景。
4. Q3/Q4 停止条件为全部计算节点的最大干基含水率首次低于 0.15。
5. Ce、h、hm、rho cp 是题设尺度下的有效闭合量；不显式加入潜热，不声称完整多相守恒。
6. Q4 采用材料坐标、长度不变和径向仿射收缩；干基变量不增加几何浓缩项。

## 数值与交付门禁

| 层次 | 门禁 |
|---|---|
| 方程 | 公式、开尔文转换、物性及解析导数逐项核对 |
| 空间 | 倍增网格比较全部正式时间、固定输出半径和动态表面 |
| 时间 | 同网格收紧 BDF、减半最大步长、Radau 对照 |
| 守恒 | 共享面通量、中心处理、总体与逐控制体平衡 |
| 事件 | 全域最大值、包围区间、根残差、根前/根/根后状态 |
| 工作簿 | 普通 Python 生成，真实数值类型、四位显示、逐格回读 |
| 图表 | 每张 PDF 对应 CSV，生成器与证据采用版本化哈希 |
| 便携性 | LF 干净检出和无 Git ZIP 均执行只读审计 |

## 独立审议返修

| 编号 | 返修事项 | 实施方案 |
|---|---|---|
| R1 | CRLF/LF 导致结果摘要误拒绝 | 文本统一 sha256-text-lf-v2，二进制保留 raw-v1，迁移旧清单并保留篡改拒绝测试 |
| R2 | Q4 时间误差与动态表面缺失 | 空间、时间、方法、根分组；22 列含动态表面并比较统一端点 |
| R3 | 插值和长期输入仅停留在计划 | 正式计算 PCHIP、半径上下半刻度、末值环境情景 |
| R4 | C 组只有大于 72 h | 延长固定半径附录4至真实事件，保存终态并计算完整交互 |
| R5 | 文档单位、网格、插值和状态冲突 | 表3–4统一小时，Q3唯一 N=40960，Q4线性主方案，移除历史 PR 状态 |
| R6 | Excel 依赖作者端私有工具 | Q1–Q4默认 openpyxl，保留独立全量回读 |

完整证据见 reports/INDEPENDENT_REVIEW_REMEDIATION.md。

## 论文主图

1. 共同模型与四问递进路线。
2. 环境、半径观测和延拓。
3. Q1/Q2 早期响应。
4. Q3/Q4 物理半径剖面与移动表面。
5. 表面、平均、全域判据和数值层。
6. A–D 物性几何机制与结构敏感性。

单问收敛、阈值局部、物性轨迹和水分平衡放在支撑材料。

## 剩余竞赛工作

1. 按当届正式要求选择 Typst 或 LaTeX 模板。
2. 将现有模型规范、结果表和六张主图压缩进论文正文。
3. 最后撰写摘要，明确 Q3/Q4 时长、有效模型边界和结构情景。
4. 核对参考文献、图表引用、页数、匿名要求和提交文件名。
5. 编译 PDF 并做逐页视觉与数值一致性验收。

数值仓库完成不等于正式论文完成；最终提交规则必须以比赛当届通知为准。
