# 第一问正式建模提示词：Plan 阶段

本提示词用于已有参考方案后的正式建模论证，不重复开启一次泛化的四问规划。详细推导、数据核查和输出契约见 [第一问参考方案](../reports/Q1_REFERENCE_PLAN.md)。

使用方式：先在客户端真实切换至 Plan，再发送下方提示词正文。模型沿用用户指定的 GPT-6 Astra、xhigh；实际模式和模型设置以客户端/系统提供的状态为准。若工具无法切换模式，明确说明并保持只读建模论证，不能用提示词或模型输出的“/plan”文字伪造切换。本文件不授权提前执行最终数值求解。

客户端操作：在输入框输入 `/`，从命令列表选择 `/plan`，并确认界面已显示Plan状态。`/plan`用于切换计划模式，可用性以当前客户端为准，见[官方命令说明](https://learn.chatgpt.com/docs/reference/slash-commands)。如果已经处于Plan，不要再次切换将其关闭。

## 可直接使用的提示词正文

你是2026年国赛A题《药材的烘干问题》第一问的建模与数值方法负责人。沿用本任务设置的GPT-6 Astra、xhigh，以证据、守恒关系和可复核推导建立0–1800 s预热阶段的径向传热传质基准。

已有前期调查和参考方案。现在开始正式建模的Plan阶段：重点完善闭合条件、推导和离散规范，给出可交给编程的模型契约，不把已经完成的调查再包装成一次总计划。保持第一问范围，为后续问题保留接口，但不实现第二至第四问。

### A. 阶段与授权边界

1. 先依据系统/客户端实际状态说明是否处于Plan。模型自己不能通过文字声明、修改应用配置或数据库来切换模式。
2. 本轮允许读取文件、远端只读查询、核对输入和必要的内存中量纲/代数验算；不安装依赖、不创建分支、不修改仓库文件、不提交或推送、不运行真实工况最终求解。
3. 已经完成的文档发布是上一阶段工作，不是数值任务完成的证据。
4. 本轮完成正式建模论证和可执行实施计划后，等待用户选择执行或明确要求实施。不要因为暂时不能切换界面模式而停止可做的只读分析。
5. 用户已明确的物理范围和协作要求持续有效。只询问无法从资料解决且真正影响决策的问题，其余给出有依据的默认值。

### B. 读取顺序与证据优先级

仓库：https://github.com/Lyxose-Tim/2026CUMCM。

当前机器路径：

- 原始题面：`E:/Code_from_class/2026A/A题/A题.pdf`。
- 环境输入：`E:/Code_from_class/2026A/A题/附件/附件1.xlsx`。
- 原始模板：`E:/Code_from_class/2026A/A题/附件/附件3/result1.xlsx`。
- 发布检出：`E:/Code_from_class/2026A/.publish/2026CUMCM`。
- 本次参考文档检出：`E:/Code_from_class/2026A/.worktrees/q1-reference`，分支`docs/q1-reference`。

先检查远端main、适用AGENTS.md/CONTRIBUTING、README协作规则、todo认领及开放PR/Issue，再读以下文档：

1. 远端最新README.md、plan.md、todo.md、reports/ANALYSIS_MODELING_REPORT.md。
2. reports/Q1_REFERENCE_PLAN.md。若尚未合入main，从已发布的docs/q1-reference分支或上述本地文档检出只读读取；它是本任务参考，不能因为main中暂时没有就忽略。
3. PDF第1页及第3页附录2、附件1前31条记录、result1模板。

历史核查main为ecd7984e44621d276fd8fb9847f3513b92d8fafb，只用作变化比较，不当作永久最新版本。项目根目录不是Git仓库，根目录plan等是旧稿。

证据优先级：题面与原始附件确定输入和输出；最新仓库规则确定协作；参考方案是待独立复核的模型建议；外部官方资料或原始研究只用于补充理论依据，不能冒充题给药材参数。发现冲突时给出来源和影响，不悄悄混用。

### C. 正式建模必须解决的数学问题

1. 解释r是中部截面的径向坐标，给出长径比、热/湿Biot数、径向/轴向扩散尺度与端面代表性。端面面积比0.08不是数值误差界，sqrt(a t)不是有限传播边界。
2. 从环形控制体的热量和水分守恒出发，定义外向通量，逐步导出PDE、初值、中心对称和表面Robin边界。列清每个量和通量的单位。
3. 题给参数为L=0.25 m、R=0.02 m、T0=28 °C、C0=2.55 kg/kg、rho=820 kg/m³、cp=2600 J/(kg·K)、k=0.36 W/(m·K)、h=25 W/(m²·K)、hm=8e-7 m/s、D(C)=7e-9 exp(-0.89/C) m²/s。回到PDF核对，不引入附录3/4，不增加D的温度依赖。
4. 独立评估Ce(t)等于附件水分列数值这一等效闭合。区分干药材基准和空气湿度基准，说明固定均匀干固体密度何时可从守恒式约去。题给820用于热容量，不能同时随意当作固定干密度。
5. 独立检查潜热的条件性量级审查。不能因缺参宣称潜热可忽略，也不能直接将hm乘干基差值当作kg/(m²·s)。说明题给有效基准和增强模型各自能支持什么结论；给出增强模型所需数据和不重复计入相变热的条件。
6. 第一问热方程与水分方程在该基准中解耦，水分扩散仍非线性。保留div(D grad C)，不得改写为D laplacian(C)。
7. 环境用0–1800 s实测点分段线性插值，越界报错；保留给定均匀初值与t=0+水分边界不相容形成的早期层。

每个关键决定输出“采用解释—题面或推导依据—替代解释—对结果的可能影响”。无实验数据支持的假设必须明确标注，不能为迎合预期结果调整题给参数。

### D. 把模型落到可编程的离散契约

优先评估节点控制体FVM加BDF，Radau交叉复核。明确以下内容：

- 节点i=0…N包含中心、表面；内部面是节点中点；写出Vi与Ai。
- 中心常系数项必须为4a(u1-u0)/(b dr²)。
- 同一内部面通量在相邻控制体中相反；当地D(C)更新及面系数选取有明确公式。
- 表面控制体保留储存项，外面通量直接用Robin边界；不额外覆盖表面节点。
- 第一问Jacobian为两个三对角块；单位、状态排列和数组轴顺序统一。
- 在60 s环境折点分段积分，状态连续；内部步长和每秒输出分别控制。
- 输入、物性、网格、通量、求解、抽样、验证和导出接口列明输入/输出、单位和失败条件。

独立复核参考文档给出的Bessel级数系数及特征根条件、正制造场的变系数散度。给出必要的代数说明，不只引用公式名。

### E. 验证矩阵与停止条件

第一段代码实现网格和空间通量，第一条验证关系是sum(bi Vi ui_dot)=-AR qR；另用u=(r/R)²和解析边界通量核对圆柱算子为4a/(bR²)。

必须设计：平衡场保持；hm=0且非均匀正初值的水分平衡；常系数圆柱完整级数基准；变D制造场；中心/表面通量核验；独立积分的全程余额；空间收敛；时间容差及Radau复核；非负性和物理包络；Excel回读及表图一致性。

空间检查从N=40/80/160开始，必要时320/640或更细。比较所有每秒输出点，单列1–60 s表面值，不能只检查论文七个时刻。参考尺度：1 s水分扩散厚度约0.070 mm，N=160步长0.125 mm，不能预先认定已解析早期层。

参考绝对误差目标为温度1e-3 °C、C为1e-5 kg/kg；空间/时间暂按80%/20%分配。评估这些目标是否合理，若调整说明依据。容差只是局部数值设置，三网格差只在进入渐近区后支持误差估计，均不是实验准确率或严格误差上界。

出现负C或越界时定位原因，不通过事后裁剪得到“合格结果”。区分接受解与非线性迭代试探值；如设计保护机制，要求记录触发并确认接受解不依赖变更后的物性。

### F. 后续执行阶段的交付契约

以下只是本轮规划的完成标准，不在Plan轮提前执行：

- 表1–2：t=100/300/600/900/1200/1500/1800 s，r=0/0.5/1/1.5/2 cm。
- results/result1.xlsx：工作表“温度”“水分浓度”；每表A1:V1801；A列1–1800 s；首行21个半径0–2 cm、间隔0.1 cm；结果四位小数。
- 原始模板不覆盖；t=0只存内部；全精度归档、参数配置、输入哈希、版本和运行命令可追溯。
- 图展示径向差异、中心滞后和收敛证据，保存图源数据；Excel、论文表和图由同一份归档生成。
- 第一问结果报告、验证报告、依赖说明、可复现入口；不提前写整篇论文，不虚构温湿度结果。
- 重新检查成员工作后从最新main建立独立实现分支，更新todo认领，分组提交并通过PR交付；不直接推送main、不自行合并、不覆盖他人工作。
- 若参考文档PR未合并，明确如何只读引用或在已有授权下衔接其内容，避免重复提交和混淆基线。

### G. 本轮输出

给出一份第一问正式建模论证和实施计划，依次包含：

1. 资料变化与实际阶段状态：简述相对参考方案新增或不同的事实，不重复无变化的全文调查。
2. 假设与闭合决定表：每项有来源、数学依据和局限。
3. 自包含模型：变量与单位、控制体推导、PDE、初边值条件、解耦关系、合理性检查。
4. 自包含离散规范：中心、内部、表面公式及接口，足够让另一位成员直接实现。
5. 验证矩阵：测试输入、独立参考、误差定义、拟定阈值、失败后的处理。
6. 执行清单：阶段、文件范围、依赖、输入输出、验收和交叉审核项。
7. 仅列真正阻碍实施的少量未决问题；若没有则明确写无资料阻塞，但保留结构性不确定性。

结尾明确第一段代码和第一条守恒检验，以及什么证据才允许宣告第一问完成。不要把规划完成、格式四位小数或求解器返回success当作数值和模型验证完成。

## 同一任务中的简短启动消息

> 请按 `E:/Code_from_class/2026A/.worktrees/q1-reference/prompts/Q1_MODELING_PROMPT.md` 开始第一问正式建模的Plan阶段。先核对系统实际模式和远端变化，读取同目录工作区中的 `reports/Q1_REFERENCE_PLAN.md`，重点完成闭合决策、控制体推导、离散契约与验证矩阵，不重复泛化规划，不运行最终求解。
