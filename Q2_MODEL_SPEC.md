# 第二问变物性热湿耦合模型规范

## 1. 任务与边界

第二问从统一初值重新计算 0--72 h 的圆柱径向温度和水分浓度。正式工作簿输出 1--259200 s；内部归档保留 t=0。模型只回答第二问给定时域内的状态演化，不声称求得严格烘干终止时刻，也不覆盖第三问。

## 2. 控制方程

设径向位置为 \(r\in[0,R]\)，温度为 \(\theta(r,t)\)，水分浓度为 \(C(r,t)\)：

\[
\rho(C)c_p(C)\frac{\partial\theta}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}\left(rk(C)\frac{\partial\theta}{\partial r}\right),
\qquad
\frac{\partial C}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}\left(rD(C,\theta+273.15)\frac{\partial C}{\partial r}\right).
\]

附录 3 的四个物性公式在全时域逐节点更新：

\[
\rho=650+128C,
\quad c_p=1450+\frac{2736C}{C+1},
\quad k=0.21+\frac{0.38C}{C+1},
\]

\[
D=2.4\times10^{-3}\exp(-0.45/C)\exp[-3850/(\theta+273.15)].
\]

温度在扩散率公式中先转换为开尔文。题面没有给出潜热闭合关系，故不加入潜热项；温度方程只按题面有效热容形式解释，不称为完整焓守恒。

## 3. 初边值条件

\[
\theta(r,0)=28\ ^\circ\mathrm C,\qquad C(r,0)=2.55.
\]

中心使用对称条件 \(\theta_r(0,t)=C_r(0,t)=0\)。表面沿用第一问 Robin 边界：

\[
-k(C)\theta_r(R,t)=h[\theta(R,t)-\theta_e(t)],
\quad
-D(C,T)C_r(R,t)=h_m[C(R,t)-C_e(t)],
\]

其中 \(h=25\ \mathrm{W/(m^2K)}\)，\(h_m=8\times10^{-7}\ \mathrm{m/s}\)。

0--14400 s 的外界温度和等效水分浓度由附件 1 在相邻观测点间线性插值。正式情景在 14400 s 后保持附件 1 最后一小时观测均值；末值保持与 50 °C、0.05 kg/kg 仅作为结构不确定性对照。

## 4. 离散与求解

- 复用第一问已审计的圆柱节点控制体几何，内部面通量在相邻控制体之间共享。
- 变导热率和变扩散率在控制体面采用调和平均。
- 解析 Jacobian 包含 \(D_C,D_T,k_C\) 引起的交叉项，以及 \([\rho(C)c_p(C)]_C\) 引起的储存系数导数项。
- 在附件 1 每个观测折点处分段积分，主求解器为 BDF；收紧 BDF 和 Radau 用于独立时间误差对照。
- 空间网格按 \(N=40,80,\ldots,20480\) 倍增。比较全部正式秒级时间和 21 个输出半径，连续两级同时满足温度与水分误差预算后才确定正式网格。

## 5. 正式门禁

生成 Excel、图和报告前必须同时满足：

1. 全部单元测试通过；
2. 空间误差连续两级达标且继续下降；
3. 收紧 BDF 与 Radau 对照均满足时间误差预算；
4. 含水率严格为正，所有值有限，温度不越出初值与历史环境包络；
5. 中心对称、共享面通量守恒、表面通量方向和总体水分平衡通过；
6. 冻结为附录 2 常物性时，新求解器与第一问归档退化一致；
7. 秒级时间轴及 21 个半径无缺漏、重复或错序；
8. `result2.xlsx` 全部 10886400 个结果单元格流式回读后与四位小数源逐格完全一致。

大型工作簿曾按原方案尝试由 Artifact Tool 完整分块写入，但其内存工作簿在 16 GB V8 堆上限仍无法容纳 10886400 个数值单元格。交付流程因此使用 Artifact Tool 写入、检查并渲染 12 行格式蓝图，最终文件由 openpyxl 的 write-only 模式按相同结构流式生成。该规模性降级不改变数据源；最终文件仍以独立流式全量回读作为真实性门禁。

## 6. 可追溯性

正式归档记录输入 SHA-256、配置、依赖、生成命令、固定数值源码提交、规范化源码摘要、原始字节摘要和每个数据分块摘要。数值源码与交付源码分域追踪：数值源码变化必须重新计算；工作簿、图表或报告源码变化会更新独立交付摘要并要求重新导出和复核，不会伪装成模型重算。
