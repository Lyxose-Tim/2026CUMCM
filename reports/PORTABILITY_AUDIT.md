# 可移植性终验记录

## 审计对象

独立审计针对第二轮复审图版收口内容提交 `c181cc0` 执行。该提交已经包含四份工作簿、Q4 Radau 与结构敏感性递归证据、全部版本化报告和图源、等比例DrawIO源、300 dpi栅格场层及便携审计程序。后续完成状态提交只增加本记录，不改变上述受摘要保护的产物。

## LF 干净检出

使用 `core.autocrlf=false` 从本地仓库创建独立克隆，再运行：

```bash
python -m common.portable_audit
```

结果为 `passed: true`。另对 399 个受版本控制的 `.py`、`.json`、`.csv`、`.md`、`.txt`、JavaScript、YAML、TOML 和 `.drawio` 文件扫描原始字节，含 CR 的文件数为 0。DrawIO 图源按 `sha256-text-lf-v2` 校验。

## 无 Git ZIP

使用 `git archive --format=zip HEAD` 创建源码快照并解压到新目录；确认该目录不存在 `.git` 后运行同一命令，结果为 `passed: true`。审计没有跳过文件级验证，覆盖：

- Q1 工作簿及 75,600 个结果单元格的导出证据；
- Q2 正式 N=20480、工作簿、6 张支撑图和 3 份报告；
- Q3 正式 `tight_N40960`、57.4740097759 h 事件、工作簿、4 张图和 3 份报告；
- Q4 正式 `main_appendix4_shrink_N20480`、51.0920106837 h 事件、工作簿、5 张图、Radau 方法对照、5 个结构情景和 4 份报告；
- 6 张论文主图、17 份实际绘图 CSV 和可编辑 DrawIO 源；
- 完整测试集 127 项通过，含两类非成功状态往返、Radau 缺失和敏感性证据损坏的拒绝测试。

第二轮图版专项核对确认图4的含水率场、移动边界和径向剖面三份CSV与上一提交的Git blob完全一致；场层栅格化后分别以100、160和260 dpi渲染检查，未再出现网格接缝，矢量等值线文字、共同时间范围和终止后空白标注均可读。

## 结论

本次终验同时证明证据链不依赖作者端 Git 状态，也不依赖 CRLF 工作区；Radau 与敏感性证据不会因进入报告或绘图阶段而绕过校验。它证明已提交文件之间的一致性与可读取性，不替代实物实验验证，也不把结构情景解释为统计置信区间。
