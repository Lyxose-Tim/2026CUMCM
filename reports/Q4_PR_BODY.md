# fix: 完成四问独立审议综合返修与可复现交付

## 摘要

本变更保留四问现有一维径向有效模型，修复独立审议发现的跨平台证据、Q4 验证缺项、结构敏感性、文档冲突和普通环境导出问题。

## 关键结果

- Q3 固定半径全域达标时间：57.4740097759 h。
- Q4 附录4与收缩半径正式时间：51.0920106837 h。
- Q4 正式临界半径：1.2000 cm；这是观测窗内插值值，不是限幅下限。
- 附件2半径完整覆盖0–72 h，末个观测值为1.198 cm；正式计算不作半径外推。
- A/B/C/D 同网格时间：57.4748038075、25.2440798099、129.8489535563、51.0921207851 h。
- C 组已延长到真实事件，不再用大于 72 h 的删失柱替代。

## 数值验证

- Q4 空间 N=10240→20480：临界时间差 0.0801930881 s，最大含水率场差 1.78429e-6。
- Q4 同 N=20480 时间设置：临界时间差 0.0046581918 s。
- BDF 与 Radau：临界时间差 0.0046461302 s。
- 比较覆盖全部共同60 s时刻、21个固定物理半径与动态表面；24 h只作中间检查点。
- BDF/Radau在共同近根时刻183900 s比较14个有效T/C列（含动态表面），时间重建误差为0，最大C差2.37917e-9。
- 正式根区间宽度 7.49368e-6 s，根残差 1.11e-16。
- 固定域退化到 Q3：时间差 0.0111871410 s，含水率最大差 3.28445e-9。

## 结构情景

- PCHIP 相对线性：-12.6059 s。
- 半径记录上下半显示刻度：事件时间范围宽 270.1814 s。
- 4 h 后环境末值保持相对末小时均值：-964.0820 s。

这些值是确定性结构情景，不是置信区间，也不与数值误差合并。

## 可复现性与交付

- 文本摘要升级为 UTF-8、LF 规范化的 sha256-text-lf-v2，二进制保持 raw-v1。
- Q1–Q4 工作簿默认由普通 Python/openpyxl 生成并立即独立回读。
- Q4 剖面补入真实动态表面，过程图取消双 y 轴；A–D、数值层和结构情景分别成图。
- 新增六张论文主图、逐图 CSV、DrawIO 技术路线源和版本化清单。
- 新增 common.portable_audit，用于 LF 干净检出和无 Git ZIP 的只读验收。
- Radau、五个结构情景及其 run.json、NPZ、accepted_steps 均递归校验；删除或篡改证据会使验证、报告、绘图和便携审计失败。
- 全量测试 `127 passed`；当前工作树、LF全新克隆和无Git archive快照的便携审计均为 `passed: true`，399个受控文本文件的CR数量为0。

## 第二轮复审收口

- 第二轮独立复审已确认F1–F4全部关闭，本次不修改主方程、正式算例或关键数值。
- 图1采用独立等比例坐标绘制圆柱截面，并疏开中心、表面与热湿通量标注。
- 图4仅栅格化300 dpi含水率场层，轮廓和文字保持矢量；两热图统一到57.474 h，Q4终止后空白明确标注，白线直接注明 `C=0.15`。
- 图4 PDF由约2.75 MiB降至约0.08 MiB，所用CSV、正式事件末点和动态表面剖面没有变化。
- 第三轮独立审核维持通过与建议合并结论；其唯一非阻塞建议已关闭：`REPRODUCE.md`现已列明三种可接受中文字体、预检命令、缺字体报错及已提交PDF的查看条件。

## 模型边界

Ce、h、hm 与 rho cp 仍是题设尺度下的有效闭合量；不显式加入潜热，不把经验 rho 当成独立干骨架密度。4 h 后环境延拓属于情景假设，Q4半径只在0–72 h观测点之间插值。当前附件没有内部温湿实测标签，不声称实验准确率。

## 复现

完整命令和产物含义见 REPRODUCE.md。Q3完整数值重建的正确入口依次为：

```bash
python -m q3.run --data-root <A题目录> --workers 1
python -m q3.refine --data-root <A题目录>
python -m q3.validation
python -m q3.export --data-root <A题目录>
python -m q3.figures
python -m q3.reports
```

Q4一键入口为 `python -m q4.reproduce --data-root <A题目录> --workers 1`。提交前应运行完整测试、Q3/Q4验证、四份工作簿回读、全部图表生成、报告生成和 `python -m common.portable_audit`。

本文件同步作为 PR #9 正文来源；本次返修更新既有 PR，不自动合并。
